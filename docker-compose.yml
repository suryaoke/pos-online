services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pos-online-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
    networks:
      - pos-online-network
    depends_on:
      - mysql
      - redis

  nginx:
    image: nginx:alpine
    container_name: pos-online-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - pos-online-network
    depends_on:
      - app

  mysql:
    image: mysql:8.0
    container_name: pos-online-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${DB_DATABASE:-pos_online}
      MYSQL_USER: ${DB_USERNAME:-laravel}
      MYSQL_PASSWORD: ${DB_PASSWORD:-password}
    volumes:
      - mysql-data:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - pos-online-network

  redis:
    image: redis:alpine
    container_name: pos-online-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - pos-online-network

networks:
  pos-online-network:
    driver: bridge

volumes:
  mysql-data:
